import java.util.ArrayList;
import java.util.List;

public class Square extends Rectangle {
    public Square(Vector referencePoint, double sideSize) {
        super(referencePoint, sideSize, sideSize);
    }
}
